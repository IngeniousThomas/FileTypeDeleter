## **Prerequisites**  

### **🔹 macOS & Linux**  
1️⃣ **Open Terminal** and navigate to the folder where `FTD_suless.sh` or `FTD_su.sh` is located.  

2️⃣ **Make the script executable** (only needed once on macOS/Linux):  
   - **For `FTD_suless.sh` (Command-line version):**  
     ```bash
     chmod +x FTD_suless.sh
     ```
   - **For `FTD_su.sh` (GUI version with `dialog`):**  
     ```bash
     chmod +x FTD_su.sh
     ```

3️⃣ **Run the script using one of these methods:**  
   - **Using `./` (recommended):**  
     - For `FTD_suless.sh`:  
       ```bash
       ./FTD_suless.sh
       ```
     - For `FTD_su.sh`:  
       ```bash
       ./FTD_su.sh
       ```
   - **Using `bash` (alternative method, useful if execution permissions are missing):**  
     - For `FTD_suless.sh`:  
       ```bash
       bash FTD_suless.sh
       ```
     - For `FTD_su.sh`:  
       ```bash
       bash FTD_su.sh
       ```
   **(You need to run this every time you want to launch the tool)**  

---

### **🏁 Windows**  
#### **🔹 Batch Version (`FTD_Windows.bat`)**
1️⃣ **Navigate to the folder** where `FTD_Windows.bat` is located.  
2️⃣ **Run the script** using one of these methods:  
   - **Double-click** `FTD_Windows.bat` to launch it.  
   - **OR** open **Command Prompt (CMD)**, navigate to the script folder, and run:
     ```cmd
     FTD_Windows.bat
     ```

#### **🔹 PowerShell Version (`FTD_Windows.ps1`)**
1️⃣ **Navigate to the folder** where `FTD_Windows.ps1` is located.  
2️⃣ **Run the script** using one of these methods:  
   - **Right-click** `FTD_Windows.ps1` → Click **"Run with PowerShell"**.  
   - **OR** open **PowerShell**, navigate to the folder, and run:
     ```powershell
     .\FTD_Windows.ps1
     ```
3️⃣ **If execution is restricted**, enable scripts temporarily by running:
   ```powershell
   Set-ExecutionPolicy Unrestricted -Scope Process
   ```
   (Only needed if PowerShell blocks execution)

---

✅ **Enjoy!** Now you're ready to use **File Type Deleter** on **macOS, Linux, and Windows**! 🚀
